{
  "passive_parse_mode": {
    "description": "被动解析模式",
    "type": "string",
    "default": "global",
    "options": ["global", "local"],
    "hint": "全局(global)：所有群聊均启用被动解析，/bili 自动下载 命令失效。局部(local)：仅在启用了自动下载的群聊中解析，通过 /bili 自动下载 on/off 控制"
  },
  "enabled_groups": {
    "description": "启用群号（局部模式）",
    "type": "list",
    "default": [],
    "hint": "仅在局部模式下生效。需要启用被动解析的群号列表。也可以通过 /bili 自动下载 on/off 命令动态添加/移除"
  },
  "send_video": {
    "description": "发送视频文件",
    "type": "bool",
    "default": true,
    "hint": "解析或下载时同时发送视频文件（需要FFmpeg）"
  },
  "send_cover": {
    "description": "文字消息附带封面图",
    "type": "bool",
    "default": true,
    "hint": "仅 渲染为图片卡片关闭 时生效：关闭则只发纯文字，开启则额外附带封面图片"
  },
  "render_as_image": {
    "description": "渲染为图片卡片",
    "type": "bool",
    "default": true,
    "hint": "将视频信息渲染为HTML图片卡片发送，关闭则发送文字+封面图"
  },
  "cache_ttl": {
    "description": "去重缓存时间（分钟）",
    "type": "int",
    "default": 5,
    "hint": "同一链接在此时间内不重复解析，0=关闭"
  },
  "auto_download_max_duration": {
    "description": "视频最大时长（分钟）",
    "type": "int",
    "default": 10,
    "hint": "超过此时长不下载/发送视频文件"
  },
  "max_video_size_mb": {
    "description": "视频最大大小（MB）",
    "type": "int",
    "default": 100,
    "hint": "超过此大小不下载/发送视频文件"
  },
  "download_restriction_mode": {
    "description": "下载限制模式",
    "type": "string",
    "default": "both",
    "options": ["duration_only", "size_only", "both", "either", "none"],
    "hint": "仅时长: 只检查时长限制 | 仅大小: 只检查大小限制 | 都不超过: 时长和大小均不超过限制才下载 | 满足一个: 时长或大小任一不超过即可下载 | 不限制: 不做任何限制"
  },
  "show_download_prompt": {
    "description": "下载前显示提示信息",
    "type": "bool",
    "default": true,
    "hint": "开启后下载视频前会显示视频标题和预估大小"
  },
  "show_download_fail_reason": {
    "description": "显示无法下载的原因",
    "type": "bool",
    "default": true,
    "hint": "开启后当视频因时长/大小限制无法下载时，会告知用户具体原因"
  },
  "retract_download_prompt": {
    "description": "撤回下载提示",
    "type": "bool",
    "default": false,
    "hint": "开启后下载提示消息将在一分钟后尝试撤回，同时提示信息中会加入(该消息一分钟后撤回)字样"
  },
  "download_all_pages": {
    "description": "下载多P视频的所有P",
    "type": "bool",
    "default": false,
    "hint": "开启后尝试下载视频的所有P（最多10个），每个P独立检测时长/大小限制；关闭则只下载1P"
  },
  "video_quality": {
    "description": "视频下载清晰度",
    "type": "string",
    "default": "480P",
    "options": ["360P", "480P", "720P", "1080P"],
    "hint": "选择下载视频的最高清晰度"
  },
  "parse_template": {
    "description": "解析消息模板",
    "type": "text",
    "default": "📺 {title}\nUP: {up_name}\n时长: {duration}\n发布时间: {pub_time}\n播放: {view}  点赞: {like}  弹幕: {danmaku}\n简介: {desc}\n链接: {link}",
    "hint": "支持变量: {title} {up_name} {link} {bvid} {aid} {duration} {pub_time} {view} {like} {danmaku} {reply} {favorite} {coin} {share} {tname} {desc}"
  },
  "opus_try_article": {
    "description": "opus 尝试转专栏(建议开启)",
    "type": "bool",
    "default": true,
    "hint": "当识别到 /opus 时，尝试转为 /read/cv 获取全文。仅对专栏类动态生效，普通图文无效"
  },
  "bilibili_cookies": {
    "description": "B站Cookies",
    "type": "text",
    "hint": "可选手动填入，也可通过 /bili 登录 扫码自动填充"
  }
}
